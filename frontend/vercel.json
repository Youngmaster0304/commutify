{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/((?!static|favicon|logo|manifest|robots).*)", "destination": "/index.html" }
  ]
}
